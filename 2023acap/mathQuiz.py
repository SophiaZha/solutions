from typing import List
class mathQuiz:
    def getBinaryOrder(self, nums:List[int]) -> List[int]:
        

        return nums

so = mathQuiz()
ints = [1,2,3,4]
print(so.getBinaryOrder(ints))

ints = [3, 15, 7, 3,2]
print(so.getBinaryOrder(ints))